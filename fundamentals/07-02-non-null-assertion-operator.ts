let courseId2: number | null; 
courseId2!.toString(); 
//The exclamation mark is called the non-null assertion operator.What it does is that it tells typescript that you know that this value is not null or undefined.
//If you hover over the courseId2 variable, you will see that the type is number | null. 