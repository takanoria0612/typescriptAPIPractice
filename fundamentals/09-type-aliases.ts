type CourseStatus = 'new' | 'in-progress' | 'completed';

let courseStatus: CourseStatus = 'new';

let newStatus:CourseStatus = 'completed'; 

type Course2 = {
    title: string,
    subtitle: string,
    lesssonsCount: number
};

